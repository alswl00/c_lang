#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

struct std {
	int scr1;
	int scr2;
	int scr3;
	int re;
};

int main() {
	int num;
	int *p = NULL;
	printf("학생수 입력 : ");
	scanf("%d", &num);
	p = (int *)(malloc(sizeof(int)*num));
	struct std[];
	for (int i = 0; i < num; i++) {
		printf("점수 1 입력 : ");
		scanf("%d", &s1.scr1);
	}
}